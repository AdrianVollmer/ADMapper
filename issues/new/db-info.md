In the "Tools" menu, add an item that says "Manage DB" or something

It should open a modal showing some basic DB stats, like total nodes, total
edges, user count, computer count, domain count, etc., size of the DB file.

Also a button for clearing the database.
