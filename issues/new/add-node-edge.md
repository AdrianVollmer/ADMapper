In "Edit", add items "Add Edge" and "Add Node".

It should open a modal. In "Add Edge" modal, show two edit fields similar to the
"Find Node" field, with a search function. Between them a dropdown for edge
types (searchable).
