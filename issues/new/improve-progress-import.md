When import data, the progress bar can be improved. Currently we have on stage
per file. But each file has "sub stages": first all nodes, then all edges. This
could give the user a better sense of progress.
