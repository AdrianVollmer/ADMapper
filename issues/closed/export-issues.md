When exporting to PNG, I can't see nodes or edges. Only labels.

In the SVG I see nothing at all, even though the information is present in the
SVG.

Also, two save dialogs are opening when clicking on one of the export formats.
