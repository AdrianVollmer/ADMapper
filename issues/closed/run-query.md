When clicking Tools-\>Run Query, a modal should open. It should offer
generous space to type in a cypher query. If CozoDb has been selected,
show a hint that datalog must be used. In either case, include a link to
the docs of cypher/datalog.

The text area should not be empty, but show the last query that was run
according to the query history.
